-- Placeholder: this migration was originally mis-ordered (altered `ReturnRequest` before it existed).
-- Real statements were moved to `20260422103000_category_indexes_return_wallet_updatedat`.
-- If your database still has a checksum mismatch for `20260421210339_new`, remove its row from
-- `_prisma_migrations` once, then run `npx prisma migrate dev` again.
SELECT 1;
